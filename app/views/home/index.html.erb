<h1>Sistema de Galpões e Estoque</h1>

<h2>Galpões</h2>

<% @warehouses.each do |w| %>
  <%= w.name %>
  Código: <%= w.code %>
  Cidade: <%= w.city %>
  <%= w.area %> m²
<% end %>

<% if @warehouses.empty? %>
  <p>Não existem galpões cadastrados</p>
<% end %>